[build]
builder = "NIXPACKS"
buildCommand = "cd medusa-backend && npm install && npm run build"

[deploy]
startCommand = "cd medusa-backend && PORT=$PORT npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[deploy.healthcheck]]
path = "/health"
timeout = 300
