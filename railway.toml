[build]
builder = "NIXPACKS"
buildCommand = "cd medusa-backend && npm install && npm run build"

[deploy]
startCommand = "cd medusa-backend && chmod +x start.sh && ./start.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[deploy.healthcheck]]
path = "/health"
timeout = 300
